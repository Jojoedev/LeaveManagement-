@model IEnumerable<Requester>
<h3> List of Departments</h3>

<table class="table table-stripped">
    <thead>
    <tr>
        
        <th>Name</th>
        
    </tr>
            
     </thead>
      <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td> @item.Name </td>
                
                <td>            
                    <a asp-action="Delete" asp-route-id="@item.Id" onclick=" return confirm('Are you sure you want to delete this?')" class="btn btn-danger"> <i class="fa-solid fa-trash"></i></a>
                </td>

            </tr>
        }
    
    </tbody>
</table>
<a href="/Requesters/Create" class="btn btn-outline-primary" >Create</a>
<div>